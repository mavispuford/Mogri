<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Mogri.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:Mogri.Views"
    Shell.FlyoutBehavior="Disabled">

    <TabBar Shell.TabBarUnselectedColor="{StaticResource Primary050}"
            Shell.TabBarForegroundColor="{StaticResource Primary}"
            Shell.TabBarTitleColor="{StaticResource Primary}">
        <Tab Title="Generate"
             Route="MainPageTab">
            <Tab.Icon>
                <FontImageSource Glyph="&#xef42;" FontFamily="{StaticResource IconFontRegular}" />
            </Tab.Icon>
            <ShellContent ContentTemplate="{DataTemplate views:MainPage}" />
        </Tab>
        <Tab Title="Canvas"
             Route="CanvasPageTab">
            <Tab.Icon>
                <FontImageSource Glyph="&#xe3f4;" FontFamily="{StaticResource IconFontRegular}" />
            </Tab.Icon>
            <ShellContent ContentTemplate="{DataTemplate views:CanvasPage}" />
        </Tab>
    </TabBar>

</Shell>
